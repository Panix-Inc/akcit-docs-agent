# Build / dev tooling
node_modules/
dist/
coverage/
.DS_Store

# Captured documentation (generated per-run, not source code)
docs/

# Auto-generated tech skills (per-tech knowledge bases) — see README "Skill por Tecnologia".
# These are produced by `capture` (project-scoped install) AND by `install-skill --local`.
# All four client integration directories are entirely auto-generated; track via templates.ts
# + scripts/sync-assets.ts, never by hand-editing the dirs.
.claude/
.agents/
.codex-plugin/
.codex/
.cursor/
.gemini/

# MCP config (re-generated by installer / sync-assets)
.mcp.json

# Stray client-context overrides (case variants seen historically)
.CLAUDE.md
.GEMINI.md
.AGENTS.md
.Cursor.md
.Codex.md

# Smoke-test outputs
test-adk-output-full/
test-adk-output/
